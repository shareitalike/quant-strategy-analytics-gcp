version: "3.8"

services:
  quant-dashboard:
    image: quant-dashboard:latest
    container_name: quant-dashboard
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: always
